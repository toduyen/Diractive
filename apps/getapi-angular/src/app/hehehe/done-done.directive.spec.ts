import { DoneDoneDirective } from './done-done.directive';

describe('DoneDoneDirective', () => {
  it('should create an instance', () => {
    const directive = new DoneDoneDirective();
    expect(directive).toBeTruthy();
  });
});
